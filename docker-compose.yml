version: "3.9"

services:
  dentist-assistant:
    build: .
    container_name: dentist-assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
